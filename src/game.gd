extends Node2D

enum MaskColor {
	RED,
	BLUE,
	YELLOW,
	GREEN,
	NO_COLOR,
}

const Masks: Dictionary = {
	"RED MASK": MaskColor.RED,
	"BLUE MASK": MaskColor.BLUE,
	"YELLOW MASK": MaskColor.YELLOW,
	"GREEN MASK": MaskColor.GREEN,
	"NO MASK": MaskColor.NO_COLOR,
}
